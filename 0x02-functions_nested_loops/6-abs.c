#include "main.h"

/**
* _abs - is a function that calculate the absolute value
*
* @int: is the num
* Return: n which it's the absolute value
*/
int _abs(int)
{
	int n;

	if (n < 0)
	{
		n = (n / -1);
		return (n);
	}
	else
		return (n);
}
